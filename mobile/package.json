{
  "name": "three-floor-chess-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor wrapper that ships the Three-Floor Chess web game as a native Android/iOS app.",
  "scripts": {
    "prepare-www": "node prepare-www.js",
    "sync": "npm run prepare-www && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
